# Weekly Report – 2026-07-22 to 2026-07-24

## Achievements

### Bugfixes & Features (v0.4.14)

| # | Item | Before | After |
|---|------|--------|-------|
| 17 | Multi-Author OR Matching | 0–2 papers (AND-only) | **39 papers**, 14–28s |
| 17 | `"Identify: X, Y, Z"` code guard | Not routed correctly | `metadata_list` with comma-split authors |
| 17 | Metadata LLM Bypass | LLM filtered OR results to AND | `paper_count=0` → Final Answer Sanitizer passthrough |
| 17 | Pre-formatted multi-author output | `metadata_query.py` pipe format | Markdown `1. **Title**` with Authors/Year/Journal |
| – | `gpt-oss` zombie reference | Hidden in KR `metadata_model_config` | Replaced with `qwen2.5:14b` |
| – | `rewritten_query` fallback | Empty → KR searched with "" | Falls back to `sys.query` |

### Embedding Model Evaluation

| Model | Quality | Latency (KR) | Verdict |
|-------|---------|-------------|---------|
| `nomic-embed-text-v2-moe` (current) | Baseline | 66–174s | ✅ Retained |
| `bge-m3` | Equivalent | 116–206s (+48%) | ❌ Rejected |
| `qwen3-embedding` | Not yet tested | – | ⬜ Planned (4× larger, higher quality expected) |

- 4 hybrid weight ratios tested (0.7/0.3 → 0.1/0.9): no effect on miCLIP/MeRIP recall
- Root cause: embedding-model limitation, not weighting or LLM issue
- Next: evaluate `qwen3-embedding` ( ~4× larger than nomic, ~1.8 GB → ~8 GB) — significantly more parameters should improve retrieval ranking quality, especially for method-level concepts like miCLIP/MeRIP
- Full report: `docs/embeddings.md`

### H100 LLM Upgrade — Preparation

- Ollama server started on `gpu-g5-1` (H100, 94 GB) via `scripts/start_ollama.sh`
- Models pulled: `qwen3.5:35b` (24 GB), `qwen3.5:122b` (81 GB), `qwen3-embedding`
- Models registered in Dify (Ollama provider, individual H100 endpoints)
- YAML migration attempted → reverted: provider wiring needs second Ollama instance or endpoint override in DSL

### Documentation

| File | Changes |
|------|---------|
| `docs/roadmap.md` | v0.4.14 status, #17 ✅, H100 plan, #16 🟡 planned, memory expansion 🟡 planned, tally updated |
| `docs/embeddings.md` | New: formal nomic vs bge-m3 comparison (267 lines) |
| `docs/test-cases.md` | #17 updated from ❌ to ✅ |
| `README.md` | v0.4.14 snapshot, tally, embedding limitation noted |
| `CHANGELOG.md` | (unchanged — v0.4.14 entry pending) |

### A2 vs H100 Timing Benchmarks

Identical model (`qwen2.5:14b`) across hardware, per `docs/timing.md`:

| Intent | A2 (16 GB) | H100 (94 GB) | Speedup |
|--------|-----------|-------------|---------|
| `metadata_list` | 55.5s | 24.5s | **2.3×** |
| `knowledge_retrieval` | 90.3s | 32.0s | **2.8×** |
| `author_lookup` | 134.8s | 37.3s | **3.6×** |

LLM inference is the dominant bottleneck; H100 eliminates the A2's memory bandwidth limit. Heavier prompts benefit disproportionately.

### Lessons Learned

- **LLM Bypass Pattern**: code-level guard → `paper_count=0` routing → pre-formatted output. Proved reliable where 5+ prompt iterations failed.
- **Query Expansion is fragile**: Hardcoded method names tested, worked, then reverted — same overfitting mistake as v0.4.0 HyDE rewriter.
- **Embedding eval needs control group**: `metadata_list` (Dataset API) queries are unaffected by embedding changes → natural control.
- **Document negative results**: `docs/embeddings.md` prevents redundant re-evaluation.

---

## Planned for Next Week

### H100 LLM Testing (Priority 2)

- [ ] Resolve provider wiring: add second Ollama provider in Dify or set endpoint per-model in YAML
- [ ] Regression test with `qwen3.5:35b`: all 20 cases, focus on:
  - #5 `entity_lookup`: m6A recall (currently missing with 14B)
  - #3 `knowledge_retrieval`: method coverage
  - #4 `author_lookup`: recall improvement (currently 27%)
- [ ] If 35B passes: test `qwen3.5:122b` with reduced context

### New Features (Priority 3)

- [ ] **#16 Collaboration Analysis**: Multi-author intersection queries ("Wer hat mit wem publiziert?") — feasible since v0.4.14 OR-matching
- [ ] **Conversation Memory Expansion**: Evaluate extending `memory` from papers-only to authors + entities for multi-turn stability
- [ ] **qwen3-embedding Evaluation**: Test qwen3-embedding (~4× larger than nomic) for retrieval quality — especially miCLIP/MeRIP recall. Larger embedding models generally produce more discriminative vectors, which should improve ranking of method-level concepts where nomic currently underperforms.

### Known Limitations (no action planned)

- `entity_lookup` m6A missing — awaiting 35B/122B test
- miCLIP/MeRIP retrieval gap — embedding-model limitation (documented)
- Externes Autor-Wiki (#18) — architectural, out of scope
